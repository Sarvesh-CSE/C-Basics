#include <stdio.h>
int main()
{
    int y;
    int x = 2;
    y = (x >= 6) ?  6 : x; /* This is equivalent to:  if (x >= 6)    y = 6;  else    y = x; */
    printf("y =%d ",y);
    return 0;
}
#include<stdio.h>
int main()
{
    int a,b;
    printf("enter value of a and b");
    scanf("%d,%d",&a,&b);
    (a%b==0)?(printf("a is multiple of b")):(printf("a is not multiple of b"));
    return(0);
}
                                             
                                             
                                                                                                                                  
                                             

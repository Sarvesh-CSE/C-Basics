#include<stdio.h>
#include<conio.h>
int main()
{
  int num,x;
  float*studentsgrades:
  printf("how many students?");
  scanf("%d,&num");
  studentgrades =(int*) malloc(num*sizeof(float));
  for(x=0;x<=num;x++)
  {
    (*studentgrades)=100;
    studentgraddes++;
  }
  return 0;
}
    
